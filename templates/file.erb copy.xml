<% if @broadcastclient == true -%>
    broadcastclient <%= @value1 %>
<% endif -%>

<% required value="a.complicated_function()" %>
    A has a complicated value : <%= value %>
<% endrequired %>


<% cache 'sidebar', 300 %>
<div class="sidebar">
</div>
<% endcache %>

#<% @someDict.each do |val| -%>
#    Some stuff with <%= val %>
#<% end -%>
#Just format that line to move do to front and replace it with for

    # regex = r'^.*(\d+\.\d+)% "\|(.*)\|$'
    regex = r'^.*(\d+\.\d+)% (do*)$'
    matches = re.findall(regex, template_str)
    # matches = re.findall("\|val\|", template_str)
    logging.info(f"Findall: {matches}")
    # for matches in re.findall("\|+?\|", template_str):
    #     logging.info(f"Matches: {matches}")